# pdxmaximalist-web

## Flyer Gallery

After adding new `.heic` files to `public/flyers/`, run:

```
npm run convert-heic
```

Converts HEIC photos to JPEG and deletes the originals. The gallery at `/flyers` automatically includes all `.jpg`, `.jpeg`, and `.png` files in that folder.
